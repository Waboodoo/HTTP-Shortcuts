package ch.rmy.android.framework.ui

/**
 * Marks an activity as an entrypoint to the app, meaning that it is possible and intended to open
 * the app via this activity. Activities which can only be opened via other activities must not
 * implement this interface.
 */
interface Entrypoint
