<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_shell"
        android:label="HTTP Shortcuts Shell">
        <activity
            android:name=".ShellActivity"
            android:exported="true"
            android:theme="@style/ShellTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="target_uri"
                android:value="http-shortcuts://shell-template-placeholder" />
        </activity>
    </application>

</manifest>
